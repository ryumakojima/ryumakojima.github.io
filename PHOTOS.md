# Images & logo

Site: **Japan-Africa Young Professionals / Japan-Africa Young Professionals**.
(See `SPEC.md` for the full placeholder list.)

## To replace
- **Logo (circle):** `public/logo-placeholder.svg` — overwrite with your round
  logo (used in nav, footer, favicon).
- **OG / share image:** `public/og.jpg` (1200×630) — currently a crop of a
  landscape photo; optionally make a branded one.
- **Reach numbers `[◯]`** on the home page (`src/pages/index.astro` + `ja`).
- **Event photos:** none yet (pre-launch). After the first event, real photos can
  go into the **Community Wall** on the home page (photo + nickname + place).
  Note: the wall is front-end only right now — collecting posts needs a form/Sheet.

## Current photos (all from a Tanzania trip — used as atmosphere only)
Stored in `src/assets/photos/` (originals untouched in `photo/`). Shown via
`object-fit: cover` so orientation never distorts.
- Heroes: `savanna-grassland` (home), `ngorongoro-crater-view` (about),
  `side-street-shops` (what we do), `cafe-view-arusha` (events),
  `elephants-sideways-1` (get involved), `zebra-waterhole` (contact).
- About feature: `map-japan-tanzania` (Japan + African flags). Contact: `inside-the-bus`.
- Unused: teaching, motorbikes, dirt-road×2, elephants-2/woodland, food,
  road-from-bike, hippo, lion, buffalo, bumpy-road, tent, gas-station.

## Sending more photos
Name them by content (romaji or Japanese is fine), e.g. `event-tokyo.jpg`,
`soccer.jpg`. Orientation doesn't matter — they're cropped to fit their slot.
